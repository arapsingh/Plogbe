const db = require('./db.configs');
const general = require('./general.configs');
const nodemailer = require('./nodemailer.configs');
const multer  = require('./multer.configs');
module.exports = {
    db,
    general,
    nodemailer,
    multer,
};
